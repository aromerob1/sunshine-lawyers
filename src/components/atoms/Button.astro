---
const { bgColor, textColor, size, content, iconPath, iconAlt } = Astro.props;
---

<button
  class=`flex items-center justify-center rounded-standard px-[30px] py-[5px] font-poppins text-tiny font-bold ${bgColor || ""} ${textColor || ""} ${size || ""} `
>
  {iconPath && <img src={iconPath} alt={iconAlt} />}
  {content}
</button>
